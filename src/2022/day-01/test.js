const test = require('ava')
const {} = require('./')
const input = require('../../helpers/readInput')(__dirname)

test('Day 1.1', t => {})

test('Day 1.2', t => {})

test('Day 1 — Solutions', t => {})
